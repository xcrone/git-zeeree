#!/bin/sh
# Script to run after resolving conflicts during merge or rebase

# Check if there were conflicts
if [ -f .git/MERGE_MSG ]; then
    echo "Conflicts resolved. Running post-merge hook."
    zeeree resolve
fi
